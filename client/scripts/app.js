'use strict';

/**
 * @ngdoc overview
 * @name chatAppApp
 * @description
 * # chatAppApp
 *
 * Main module of the application.
 */
angular
    .module('chatApp', [
    /*'ngAnimate',
'ngAria',
'ngCookies',
'ngMessages',
'ngResource',*/
'ngRoute'

  ])
    .config(function () {
      /*  $routeProvider
      .when('/', {
          templateUrl: 'views/main.html',
          controller: 'MainCtrl',
          controllerAs: 'main'
      })
      .otherwise({
          redirectTo: '/'
      });*/
    });